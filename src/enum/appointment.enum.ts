export enum urgencyLevelEnum {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}

export enum appointmentEnum {
  PENDING = "pending",
  COMPLETE = "completed",
  CANCELLED = "cancelled",
  RESCHEDULED = "rescheduled",
}
